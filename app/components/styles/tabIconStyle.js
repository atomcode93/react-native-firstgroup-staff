import {StyleSheet} from 'react-native';
import {colors} from '../../themes';

export default StyleSheet.create({
  tabIconContainer: {
    flex: 1,
    alignItems: 'center',
  },
  tabIconText: {
    fontWeight: '500',
    padding: 2
  }
})
